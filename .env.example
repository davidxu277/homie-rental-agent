# 复制成 .env.local 后填入自己的 key：
#     cp .env.example .env.local
#
# .env.local 已被 .gitignore 排除，不会进仓库。
# 部署时不要提交这个文件 —— 在 Vercel 的 Environment Variables 面板里单独配置。

# 必需。驱动需求抽取和回复生成（Claude Sonnet 5）。
ANTHROPIC_API_KEY=

# 必需。查「房源所在站点/区域 → 用户目的地」的真实行程时间，
# 需要在 Google Cloud 里启用 Distance Matrix API。
#
# 通勤是这个产品分量最重的一条约束，房源数据本身算不出任何站间行程时间。
# 不填不会崩（整条约束降级为「这一轮没按通勤筛」并在界面标出来），
# 但那是故障时的安全网，不是推荐的运行方式。
GOOGLE_MAPS_API_KEY=
